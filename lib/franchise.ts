// Re-export shim → single source of truth is @saguaro/core (franchise triage engines).
// Do NOT add logic here; edit the package. (Fixes toScale very-high + long-lead /releas/ drift.)
export * from '@saguaro/core';
